# 🏗️ Token Launchpad Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         USER                                 │
│                    (Web Browser)                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Next.js 14 + React + TypeScript                     │  │
│  │  - LaunchpadClient                                    │  │
│  │  - TokenCreationForm                                  │  │
│  │  - TokenManagementClient                              │  │
│  │  - MyTokensList                                       │  │
│  │  - LaunchpadStats                                     │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Web3 Integration                                     │  │
│  │  - Wagmi + Viem                                       │  │
│  │  - RainbowKit (Wallet Connection)                    │  │
│  │  - TanStack Query (Data Fetching)                    │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────┬─────────────────────┘
             │                        │
             ↓                        ↓
┌────────────────────────┐  ┌────────────────────────────────┐
│   BACKEND (API)        │  │   BLOCKCHAIN                   │
│                        │  │                                │
│  Next.js API Routes    │  │  Smart Contracts               │
│  ├─ /api/launchpad/    │  │  ├─ TokenFactory.sol          │
│  │  ├─ create          │  │  ├─ StandardToken.sol         │
│  │  ├─ my-tokens       │  │  └─ TaxToken.sol              │
│  │  ├─ analytics       │  │                                │
│  │  ├─ token           │  │  Deployed on:                  │
│  │  └─ upload-logo     │  │  - Ethereum                    │
│  │                     │  │  - BSC                         │
│  │  NextAuth           │  │  - Polygon                     │
│  │  - Authentication   │  │  - Arbitrum                    │
│  │  - Session Mgmt     │  │  - Base                        │
│  └─────────────────────┘  └────────────────────────────────┘
             │                        │
             ↓                        ↓
┌────────────────────────┐  ┌────────────────────────────────┐
│   DATABASE             │  │   FEE COLLECTOR                │
│                        │  │                                │
│  MongoDB Atlas         │  │  Your Wallet Address           │
│  ├─ LaunchedToken      │  │  - Receives fees instantly     │
│  ├─ TokenAnalytics     │  │  - No withdrawal needed        │
│  ├─ User               │  │  - Automatic accumulation      │
│  └─ ChatHistory        │  └────────────────────────────────┘
└────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────────────────┐
│   EXTERNAL SERVICES (All Free Tier)                        │
│                                                             │
│  ├─ Cloudinary (Image Storage)                             │
│  ├─ Alchemy (Blockchain RPC)                               │
│  ├─ Moralis (Web3 Data)                                    │
│  ├─ CoinGecko (Price Data)                                 │
│  └─ Etherscan (Block Explorer)                             │
└────────────────────────────────────────────────────────────┘
```

## User Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    TOKEN LAUNCH FLOW                         │
└─────────────────────────────────────────────────────────────┘

1. CONNECT WALLET
   User → RainbowKit → MetaMask/WalletConnect
   
2. FILL FORM
   User → TokenCreationForm
   ├─ Token Name
   ├─ Symbol
   ├─ Supply
   ├─ Decimals
   ├─ Upload Logo → Cloudinary
   ├─ Tax Settings (optional)
   └─ Social Links
   
3. REVIEW & PAY
   User → Calculate Fee
   ├─ Base: 0.1 ETH
   ├─ Tax: +0.02 ETH
   └─ Total: 0.12 ETH
   
4. DEPLOY TOKEN
   User → Approve Transaction
   ↓
   TokenFactory.createToken()
   ├─ Deploy Token Contract
   ├─ Transfer Fee → Your Wallet ✅
   └─ Emit TokenCreated Event
   
5. SAVE RECORD
   Backend → MongoDB
   ├─ Save LaunchedToken
   └─ Create TokenAnalytics
   
6. REDIRECT
   User → Management Dashboard
   ├─ View Token Info
   ├─ See Analytics
   └─ Update Settings
```

## Fee Collection Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    FEE COLLECTION                            │
└─────────────────────────────────────────────────────────────┘

User Wallet                TokenFactory              Your Wallet
    │                           │                         │
    │  createToken()            │                         │
    │  + 0.1 ETH               │                         │
    ├──────────────────────────>│                         │
    │                           │                         │
    │                           │  Deploy Token           │
    │                           │  ─────────────          │
    │                           │                         │
    │                           │  transfer(0.1 ETH)      │
    │                           ├────────────────────────>│
    │                           │                         │
    │                           │                    ✅ Fee Received
    │                           │                         │
    │  Token Deployed ✅        │                         │
    │<──────────────────────────┤                         │
    │                           │                         │
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    DATA FLOW                                 │
└─────────────────────────────────────────────────────────────┘

Frontend                  Backend                  Database
   │                        │                         │
   │  Create Token          │                         │
   ├───────────────────────>│                         │
   │                        │  Save Record            │
   │                        ├────────────────────────>│
   │                        │                         │
   │                        │  Return Success         │
   │<───────────────────────┤                         │
   │                        │                         │
   │  Get My Tokens         │                         │
   ├───────────────────────>│                         │
   │                        │  Query Tokens           │
   │                        ├────────────────────────>│
   │                        │                         │
   │                        │  Return Tokens          │
   │                        │<────────────────────────┤
   │  Display Tokens        │                         │
   │<───────────────────────┤                         │
   │                        │                         │
   │  Get Analytics         │                         │
   ├───────────────────────>│                         │
   │                        │  Aggregate Data         │
   │                        ├────────────────────────>│
   │                        │                         │
   │                        │  Return Stats           │
   │                        │<────────────────────────┤
   │  Display Stats         │                         │
   │<───────────────────────┤                         │
```

## Analytics Update Flow

```
┌─────────────────────────────────────────────────────────────┐
│                ANALYTICS UPDATE SERVICE                      │
└─────────────────────────────────────────────────────────────┘

Cron Job (Every Hour)
    │
    ↓
Get All Deployed Tokens
    │
    ↓
For Each Token:
    │
    ├─> Moralis API
    │   └─> Get Holders
    │
    ├─> CoinGecko API
    │   └─> Get Price & Volume
    │
    ├─> Calculate Metrics
    │   ├─ Current Holders
    │   ├─ Price
    │   ├─ Volume 24h
    │   └─ Market Cap
    │
    └─> Update Database
        ├─ TokenAnalytics
        └─ LaunchedToken
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  COMPONENT STRUCTURE                         │
└─────────────────────────────────────────────────────────────┘

app/launchpad/page.tsx
    │
    └─> LaunchpadClient
        ├─> TokenCreationForm
        │   ├─ Input Fields
        │   ├─ Logo Upload
        │   ├─ Tax Settings
        │   └─ Submit Button
        │
        ├─> MyTokensList
        │   ├─ Token Cards
        │   └─ Manage Links
        │
        └─> LaunchpadStats
            ├─ Platform Metrics
            ├─ Recent Launches
            └─ Top Tokens

app/launchpad/manage/[address]/page.tsx
    │
    └─> TokenManagementClient
        ├─> Overview Tab
        │   ├─ Token Info
        │   └─ Social Links
        │
        ├─> Analytics Tab
        │   ├─ Stats Cards
        │   └─ Price Chart
        │
        └─> Settings Tab
            ├─ Update Info
            └─ Tax Settings
```

## Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                  DATABASE SCHEMA                             │
└─────────────────────────────────────────────────────────────┘

LaunchedToken
├─ tokenAddress (unique, indexed)
├─ name
├─ symbol
├─ decimals
├─ totalSupply
├─ creatorAddress (indexed)
├─ chainId
├─ hasTax
├─ buyTax
├─ sellTax
├─ logoUrl
├─ website
├─ twitter
├─ telegram
├─ description
├─ launchedAt
├─ transactionHash
├─ launchFeePaid
├─ status
└─ timestamps

TokenAnalytics
├─ tokenAddress (unique, indexed)
├─ chainId
├─ currentHolders
├─ totalTransactions
├─ price
├─ volume24h
├─ marketCap
├─ history []
│   ├─ timestamp
│   ├─ price
│   ├─ volume
│   └─ holders
├─ topHolders []
│   ├─ address
│   ├─ balance
│   └─ percentage
├─ lastUpdated
└─ timestamps
```

## API Endpoints

```
┌─────────────────────────────────────────────────────────────┐
│                    API ROUTES                                │
└─────────────────────────────────────────────────────────────┘

POST /api/launchpad/create
├─ Input: Token details
├─ Auth: Required
├─ Action: Save token record
└─ Output: Token object

GET /api/launchpad/my-tokens
├─ Input: None
├─ Auth: Required
├─ Action: Get user's tokens
└─ Output: Array of tokens

GET /api/launchpad/analytics
├─ Input: None
├─ Auth: None
├─ Action: Get platform stats
└─ Output: Stats object

GET /api/launchpad/token?address=0x...
├─ Input: Token address
├─ Auth: None
├─ Action: Get token details
└─ Output: Token + Analytics

PUT /api/launchpad/token?address=0x...
├─ Input: Updated fields
├─ Auth: Required (owner)
├─ Action: Update token info
└─ Output: Updated token

POST /api/launchpad/upload-logo
├─ Input: Image file
├─ Auth: Required
├─ Action: Upload to Cloudinary
└─ Output: Image URL
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  SECURITY LAYERS                             │
└─────────────────────────────────────────────────────────────┘

Layer 1: Smart Contract
├─ Access Control (onlyOwner)
├─ Input Validation
├─ Reentrancy Protection
└─ Emergency Pause

Layer 2: Backend API
├─ Authentication (NextAuth)
├─ Rate Limiting
├─ Input Sanitization
└─ CORS Protection

Layer 3: Database
├─ Connection Security
├─ Query Parameterization
├─ Access Control
└─ Regular Backups

Layer 4: Frontend
├─ Wallet Verification
├─ Transaction Validation
├─ XSS Protection
└─ HTTPS Only
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  DEPLOYMENT                                  │
└─────────────────────────────────────────────────────────────┘

Development
├─ Local: localhost:3000
├─ Database: MongoDB Atlas (Dev)
└─ Blockchain: Sepolia Testnet

Staging (Optional)
├─ Vercel Preview
├─ Database: MongoDB Atlas (Staging)
└─ Blockchain: Sepolia Testnet

Production
├─ Vercel Production
├─ Database: MongoDB Atlas (Prod)
├─ Blockchain: Ethereum Mainnet
└─ CDN: Vercel Edge Network
```

## Monitoring & Analytics

```
┌─────────────────────────────────────────────────────────────┐
│                  MONITORING                                  │
└─────────────────────────────────────────────────────────────┘

Application Monitoring
├─ Vercel Analytics
├─ Error Tracking (Sentry)
└─ Performance Monitoring

Business Metrics
├─ Total Launches
├─ Revenue (ETH)
├─ User Growth
└─ Conversion Rate

Blockchain Monitoring
├─ Transaction Success Rate
├─ Gas Usage
├─ Contract Events
└─ Fee Collection
```

---

This architecture provides:
- ✅ Scalability
- ✅ Security
- ✅ Reliability
- ✅ Easy maintenance
- ✅ Cost efficiency
- ✅ Fast performance
